import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const attempts = sqliteTable('attempts', {id:text('id').primaryKey(),mode:text('mode').notNull().default('workshop'),name:text('name').notNull(),email:text('email').notNull(),seed:integer('seed').notNull(),stage:integer('stage').notNull().default(0),state:text('state').notNull(),score:integer('score').notNull().default(0),revision:integer('revision').notNull().default(0),created:text('created').notNull(),completed:text('completed')}, t=>[index('idx_attempts_email_completed').on(t.email,t.completed)]);
